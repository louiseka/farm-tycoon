export type Crop = {
    name: string,
    buyPrice: number,
    sellPrice: number,
    img: string,
    alt: string
}

export type Animal = {
    name: string,
    buyPrice: number,
    sellPrice: number,
    img: string,
    alt: string,
    produce: string
}